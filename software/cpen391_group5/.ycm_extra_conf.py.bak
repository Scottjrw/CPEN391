import os
import ycm_core

flags = [
    '-Wall',
    '-std=c++11',
    '-I../cpen391_group5_bsp',
    '-I../cpen391_group5_bsp/HAL/inc',
    '-I../cpen391_group5_bsp/drivers/inc',
    '-nostdinc',
    '-nostdlib',
    '-nostdlibinc',
    '-nolibc',
    # ++ flags may not needed as we have -x c but better to be safe
    '-nostdinc++',
    '-nostdlib++'
]

def FlagsForFile( filename, **kwargs ):
    return { 'flags': flags }
